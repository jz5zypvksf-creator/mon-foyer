name: Finalize architecture extract history

on:
  push:
    branches:
      - architecture/extract-history
    paths:
      - 'scripts/apply-extract-history.mjs'

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: architecture/extract-history
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply App.jsx wiring
        run: node scripts/apply-extract-history.mjs
      - name: Commit permanent App.jsx wiring
        run: |
          git config user.name "ChatGPT Codex Connector"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx
          git commit -m "arch: permanently wire OperationHistory component into App.jsx"
          git push origin architecture/extract-history
      - name: Install dependencies
        run: npm ci
      - name: Run full test suite
        run: npm run test
      - name: Build production bundle
        run: npm run build
