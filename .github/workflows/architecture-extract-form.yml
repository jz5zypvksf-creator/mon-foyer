name: Verify architecture extract form
on:
  push:
    branches: [architecture/extract-form]
    paths: ['scripts/apply-extract-form.mjs']
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: architecture/extract-form
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Wire OperationForm
        run: node scripts/apply-extract-form.mjs
      - name: Commit permanent extraction
        run: |
          git config user.name "ChatGPT Codex Connector"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/features/operations/OperationForm.jsx src/features/operations/useOperationDraft.js
          git commit -m "arch: extract operation form and hook to dedicated features"
          git push origin architecture/extract-form
      - run: npm ci
      - name: Test
        run: npm run test
      - name: Build
        run: npm run build
